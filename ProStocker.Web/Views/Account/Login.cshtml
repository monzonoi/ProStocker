@model ProStocker.Web.Models.LoginViewModel
@{
    ViewData["Title"] = "Iniciar Sesión";
}

<div class="d-flex justify-content-center align-items-center vh-100">
    <div class="card p-4 shadow-lg" style="width: 400px; background: #0f3460; border: 2px solid #54BAB9;">
        <h2 class="text-center text-neon" style="color: #54BAB9;">ProStocker</h2>
        <form asp-action="Login" method="post">
            <div class="form-group mb-3">
                <label class="text-light">Usuario</label>
                <input asp-for="UsuarioNombre" class="form-control" placeholder="Ingrese su usuario" />
                <span asp-validation-for="UsuarioNombre" class="text-danger"></span>
            </div>
            <div class="form-group mb-3">
                <label class="text-light">Contraseña</label>
                <input asp-for="Contrasena" type="password" class="form-control" placeholder="Ingrese su contraseña" />
                <span asp-validation-for="Contrasena" class="text-danger"></span>
            </div>
            <button type="submit" class="btn btn-neon w-100">Iniciar Sesión</button>
            @if (!ViewData.ModelState.IsValid)
            {
                <div class="text-danger mt-3 text-center">@Html.ValidationSummary()</div>
            }
        </form>
    </div>
</div>

@* 
                @model ProStocker.Web.Models.DashboardViewModel
@{
    ViewData["Title"] = "Login";
    Layout = "~/Views/Shared/DashboardShell.cshtml";
}

<div class="content">
    <h1 class="mb-4" style="color: #54BAB9;">Iniciar Sesión</h1>
    <div class="card p-3 shadow-lg">
        @if (ViewBag.Error != null)
        {
            <div class="alert alert-danger">@ViewBag.Error</div>
        }
        <form asp-action="Login" asp-controller="Dashboard" method="post">
            <div class="mb-3">
                <label class="form-label">Usuario</label>
                <input type="text" name="usuarioNombre" class="form-control" required />
            </div>
            <div class="mb-3">
                <label class="form-label">Contraseña</label>
                <input type="password" name="contrasena" class="form-control" required />
            </div>
            <button type="submit" class="btn btn-neon">Iniciar Sesión</button>
        </form>
    </div>
</div> *@